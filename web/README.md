# Web

React frontend: per-segment congestion layer (GeoJSON polylines via deck.gl `PathLayer` / Leaflet
canvas), route overlays, before/after comparison dashboard, and parameter controls. See
`../docs/api.md` and `project-spec.md §R6`. Scaffold placeholder — implemented in Phase 10.
