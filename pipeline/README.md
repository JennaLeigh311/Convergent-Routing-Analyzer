# Pipeline

Spark Structured Streaming jobs (PySpark): GPS-ping map-matching and windowed per-segment congestion
aggregation. Emits the `segment-congestion` schema (see `../docs/contracts.md`). Scaffold placeholder —
implemented in Phases 6–7.
