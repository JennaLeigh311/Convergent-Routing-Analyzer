# Algorithms

> Stub. The traffic-assignment catalog: naive, reactive, incremental, MSA (user equilibrium),
> system-optimal (marginal-cost), and multipath. Includes BPR cost, the SO marginal-cost derivative,
> convergence criteria, and the Price-of-Anarchy metric. See `project-spec.md §4`, `§R5`.
