# Data Pipeline

> Stub. Kafka topics, the Spark Structured Streaming map-match + windowed aggregation job (dual trigger:
> live + batch), and windowing/watermark semantics. See `project-spec.md §R4`. The wire schema is frozen
> in `contracts.md`.
