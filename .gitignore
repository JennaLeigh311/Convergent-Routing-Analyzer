# macOS
.DS_Store

# Claude Code local files
.claude/

# Project spec (kept local / untracked)
project-spec.md

# Local env — never commit real secrets; keep the template (.env.example)
.env
.env.*
!.env.example

# Phase 8 data pipeline (issue #120): the OSM extract and the generated
# edge_attributes export are DATA BLOBS produced by data/scripts — commit the
# scripts, never the blobs. Re-generate with `make data-porto`.
data/osm/
data/out/
